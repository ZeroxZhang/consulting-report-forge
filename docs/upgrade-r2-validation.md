# R2 候选实现与原报告验证

日期：2026-09-25。运行时：1.5.0-rc.1（本地未发布）。本轮继续实施 B/F 的严格分析合同与 C 的初版影子测量；不宣称阶段2–4所有退出条件已满足。

## 找到的真实材料

原报告位于 `/Volumes/Out/临时任务/0924_mimo_forge_test/codex_retest/report`。已读取两份专项审查、工作记录、原蓝图/任务与渲染器，核对29页旧快照。真实材料证实：浅蓝灰 `#bbcbd6`、1px 底线的 RGB 极差27触发旧阈值误报；四个2px彩色顶线符合当时政策。R1 的中性伪元素夹具只是另一种重建例，不应冒充原问题。

本轮补上该浅蓝灰反例，候选识别综合线宽、亮度和实际不透明背景对比；旧生产政策仍不变。

## 首批实现（后续真实验收见下文）

| 工作 | 结果与边界 |
|---|---|
| 显式能力表 | task1/2/3、analysis-review1/2、final-review3/4/5 分流；不以 >= 推断未来版本 |
| semantic-v2 | 展品、标题、侧栏、编码、下界、分母变化失效；逐字段差异；分离字体/坐标不失效；未分离 custom 整体失效并提示 |
| 展示依赖 | `$metric` 原值引用进入派生 exhibit 与采纳依赖；显式 displayBindings 逐值核对；复用现有 external/模型血缘 |
| 集成 | 编译、装配、最终审查、聚合、快照、复用、打包接入；新草稿不可当正式审查 |
| 原报告迁移 | 新目录 `renders/upgrade-r2/biopharma/`；12份显式原文件摘要保存，8份附件复制；26个展品列为待映射复核，不猜单位或分母 |
| 原报告预览 | 严格合同下26页正文编译、29页HTML装配成功；显式分析预览，未签新 ready，未冒充正式交付 |
| 历史兼容 | 原29页 baseline 快照仍以 review4/legacy-v1 验证为 complete；1.4.0 原读取器字节作为固定夹具，拒绝 task3；旧 task 归一化结果相同 |
| 版面影子测量 | HTML行框、普通SVG字符、跨区文字相交、边界裁切、矩形不透明遮挡；screen/print 固定反例与合法对照已通过 |

合成严格任务已跑通真实三页 HTML/PDF acceptance、聚合、快照和打包，且拒绝不完整审查、改变论断后沿用旧摘要。合成测试内的署名和通过字段都是明确标记的校验器夹具，不是实际读图签署。结果在 `renders/strict-integration/result.json`。

## 本轮完整回归

首批运行记录为 `renders/upgrade-r2/verification/results.json`，以下五项均实际完成，退出码0：

| 命令 | 用时 |
|---|---|
| `npm test` | 11.30秒 |
| `npm run test:render` | 10.76秒 |
| `npm run test:upgrade-render` | 31.32秒 |
| `npm run test:upgrade-r1` | 2.67秒 |
| `npm run test:upgrade-r2` | 10.75秒 |

五项并行，用时不可当作独立基准；token未知。变更文档本地链接及 `git diff --check` 通过。最终再次核对迁移涉及的12份原文件摘要不变，原29页review4快照仍complete，新analysis-review2仍incomplete，记录为 `renders/upgrade-r2/original-verification.json`。各Agent桥接入口仍指向真源，无冗余入口。

## 原稿影子扫描与实际看图

原29页HTML单独运行影子扫描，结果在 `renders/upgrade-r2/reading-shadow/reading-shadow.json`，共13条候选集中在P05和P14；各页包含计时、覆盖声明、截图和最多5张局部证据图。

- P05的12条来自旧0–5刻度与新年份的重叠/遮挡。实际查看全页并读取渲染器确认，作者用白块遮住旧刻度后重新绘制年份；当前可见年份正常。这12条不作为当前阅读缺陷，是几何影子观测的误报/需理解绘制意图的案例。
- P14的1条是“潜在里程碑｜百万美元（不是到账）”与下方样本说明的文字框相交。实际看图可见两行过近并有相交，应在后续报告副本修订中解决；本轮未修改原报告。

13条候选不等于13个实质问题。这里仅抽看被标注的两页，并未重新人工验收整份报告或逐页PDF，也未统计其余页面的真实漏报。因此不计算/宣称通用precision、recall或零漏检。保留 shadow，不升为硬门禁。

## 真实报告完整验收续记

截至2026-09-25T15:36:25Z，已在迁移副本完成真实29页新合同链路，详见 `renders/upgrade-r2/biopharma/validation-result.json`。原12份文件摘要再次核对未变，旧29页review4快照仍complete。

- 26个展品建立86处 `exhibitBindings`，精确比较结构化值、单位、关系符、null及证据编码；定性表格/作者情景部分绑定作者登记输入，不冒充外部事实已核实。7个关键派生指标通过external指向本次可复算输出。
- 独立实例 `/root/independent_analysis` 初审集中发现5项问题：迁移模型旧路径、T05来源定位、下界派生关系与未知股权、利润标题过强、样本单位。全部修复后实际复验；双方前置审查在既有样本/情景/来源限制内通过。新模型读取当前附件，原旧代码/输出仅归档，不再进入当前生产血缘。
- 渲染器消费编译后的展品；P05直接生成年份刻度，删除白块覆盖旧数字的做法；P14为轴标题和样本说明留出不同空间；P26单位可见，增加单位后发现表尾过近，实际调整行距并复看。
- 正式编译、装配、acceptance完成：29页HTML与29页PDF，工程errors为0；screen/print影子观测均为0，但不据此宣称通用零漏检。
- 作者实际查看29页HTML和全部最终PDF；正式HTML中28页截图与作者此前逐页查看版本字节相同，变化的sample重新查看。独立实例另外实际查看全部58张最终截图。7项参考页留白/重复结构等warnings逐项处置，无未决major/blocking。
- analysis-review2、final-review5聚合均完成；正式HTML/PDF已打包至 `biopharma/delivery/`。新73文件快照加载成功，复用准备显示29页图像可继承；两个复用草稿仍为incomplete、analysisSha256为null，没有自动替新一轮签署。

分析摘要：`60efdf5b838e68656df06bc3ab13ec4586fedff1d594a32afb16da35b8342bcb`。本次未刷新全部公开行业来源；原核验时点及原资料限制保留。独立发现、复验和两份最终审查均留存实际依据；不以机器校验替代读图。

运行时据此新增显式展品附件对账与采纳血缘，迁移清单增加模型可移植性待办；新增小数、单位、关系符、null误填零及未知附件负例测试。`npm test`与`npm run test:upgrade-r2`本轮均退出0，日志为 `verification/mapping-tests.log` 和 `mapping-integration.log`。续记阶段未统一测量端到端耗时，独立记录保留实际时间戳；token成本未知。

## 本批当时尚未满足的发布条件

后续统一入口、多页参考资料和固定标注矩阵已集成，当前状态以[第三批记录](upgrade-r3-validation.md)为准。下文保留R2当时边界。

仍需：任意自定义图表的全面自动映射约束、完整人工标注集（四类密表/旋转/复杂裁切等）、统一生产写入入口、参考资料块全链路生产接入，以及定性战略和纯编辑两类真实任务试用。已有高密度定量真实案例完成新合同验收；不能由单例推出三类任务全部通过。多页资料与细线仍只是候选模块；task3当前显式沿用旧单页资料及视觉政策。

默认模板仍使用task2；rc仅提供显式试用入口。没有提交、推送或发布，没有修改原报告字节。初始migration.json和original-verification.json保留阶段历史，当前状态以biopharma/validation-result.json及最终审查为准。
